# Local conversation/session exports — never commit
Chat.txt

# Per-host Claude Code config and per-session worktrees (local dev state, not for the catalogue)
.claude/

# Local agent guidance — not shipped with the catalogue
CLAUDE.md
